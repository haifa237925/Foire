{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend pour la plateforme de livres numériques TEPTAC",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ebooks",
    "audio",
    "plateforme",
    "mern",
    "typescript"
  ],
  "author": "TEPTAC SERVICES",
  "license": "ISC",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9"
  }
}
